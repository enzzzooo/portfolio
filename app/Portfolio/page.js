import s from "../portfolio/page.module.css";
import Star from "../components/Star";
export default function page() {
  return (
    <section className={`top ${s.t}`}>
      Make the apple rounded rectangle layout here
    </section>
  );
}
